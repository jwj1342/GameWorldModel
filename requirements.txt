# GameWorldModel 的 Python 依赖。Python 3.10 以上（代码用了 X | None 这种写法）。
#
#   pip install -r requirements.txt
#   pip install --no-deps "git+https://github.com/facebookresearch/vggt"
#
# VGGT 必须加 --no-deps，它的 setup 里钉了 numpy<2 和很旧的 torch，会把环境拉坏。
# 另外需要系统里有 ffmpeg 和 ffprobe（抽帧和合成视频用），以及 Node.js 20 以上（跑 three.js 内核）。
#
# 只想编译和玩已有的场景程序、不做感知的话，装到 jsonschema 那一组就够了，torch 那几行可以跳过。

# 场景程序的校验、编译和反馈循环
jsonschema>=4.20
jsonpatch>=1.33
pyyaml>=6.0
numpy>=1.26
scipy>=1.11
pillow>=10.0

# 调模型（OpenRouter、自托管 vLLM 都是 OpenAI 兼容接口）
openai>=1.40

# 感知模型
torch>=2.4
torchvision>=0.19
transformers>=5.14      # 需要 Sam2VideoModel 和 GroundingDino，这两个是 transformers 5.x 才有的
huggingface-hub>=0.26
safetensors>=0.4
accelerate>=1.0
timm>=1.0
einops>=0.8

# 测试
pytest>=8.0
